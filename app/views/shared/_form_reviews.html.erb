<%= simple_form_for([@producer, @producer_review], remote:true) do |f| %>
    <%= f.input :content, required: true, placeholder: "Content" %>
    <%= f.input :mark, required: true, placeholder: "Mark" %>
    <%= f.button :submit %>
<% end %>
